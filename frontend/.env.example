# 이삿날 프론트엔드 환경변수 — 실제 값은 .env(로컬) 또는 Vercel Environment(배포)에 입력
# .env는 .gitignore에 포함되어 절대 커밋되지 않습니다. 이 파일에 실제 값을 적지 마세요.

# 백엔드 API 절대 URL — 배포 환경에서만 설정 (예: https://moving-day-api.onrender.com)
# 로컬은 비워두면 vite proxy가 /api → http://localhost:4000 으로 연결합니다.
VITE_API_BASE=
